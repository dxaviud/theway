{
  "name": "theway",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "watch": "pnpm tsc -w",
    "dev": "nodemon dist/index.js",
    "build": "pnpm tsc",
    "start": "node dist/index.js",
    "migration:gen": "pnpm build && pnpm typeorm migration:generate ./src/migration/migration -d ./dist/data-source.js",
    "migration:run": "pnpm build && pnpm typeorm migration:run -d ./dist/data-source.js",
    "clean": "rm -rf dist & rm -rf node_modules && pnpm i"
  },
  "keywords": [],
  "author": "David Xu",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.11.7",
    "nodemon": "^2.0.20",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "pg": "^8.8.0",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.10"
  }
}
